# Status — APK Sentinel Pipeline

**Last updated:** 2026-07-19 02:30:57

## Overall State
Pipeline is mid-build. L0 (ingestion/triage) is complete and tested. L1 (static deep-analysis) is scaffolded and jadx path is verified working end-to-end on the test APK; ghidra engine code written but not yet executed (tool just extracted).

## Stage Status
| Stage | Status | Notes |
|-------|--------|-------|
| L0 ingestion/triage | DONE | ingest.py, whitelist, threat_cache (85 hashes), dataset (49 icons+meta) |
| L1 jadx engine | WORKING | decompiles + scans; library-noise filter fixed |
| L1 ghidra engine | CODED, UNTESTED | analyzeHeadless path fixed to nested folder |
| L1 combo engine | CODED | runs jadx then ghidra, merges |
| L1 dispatcher | WORKING | l1.py reads L0 evidence.json, routes by track |
| L2 dynamic/runtime | NOT STARTED | |

## Tooling
- Java system: v8 (too old). JDK17 portable at `D:\BOI\tools\jdk17\jdk-17.0.19+10`.
- jadx 1.5.6 at `D:\BOI\tools\jadx\` (headless MUST use `jadx.cli.JadxCLI` class, not `-jar`, or GUI pops on Windows).
- ghidra 12.1.2 at `D:\BOI\tools\ghidra\ghidra_12.1.2_PUBLIC\` (analyzeHeadless.bat under support/).
- jadx-gui with-jre bundle also downloaded (`tools\jadx-jre`) but unused (GUI only).

## Open Bugs
- L1 jadx scanner initially mis-flagged app code (`com/android/...`) as library; fixed via leading-segment check. Re-run pending verification.
- InsecureBankv2 yields 0 malware-behavior findings (expected: it is a *vulnerable* app, not a banker trojan). Need a real banker sample to validate detection.
