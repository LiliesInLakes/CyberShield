# Session Summary — 2026-07-19 18:00:00

## Goal
Fix Ghidra/combo engine, tune YARA FPs, run full pipeline validation.

## Done
1. **Combo engine fixed** — was blocking on large .so (missing `-readOnly -noanalysis`). Parallel jadx + ghidra now works.
2. **Ghidra engine fixed** — `capture_output=True` pipe buffer hang → redirect to log file. Deduplicate .so by stem (multi-arch APKs). Parallel analysis with 2 workers.
3. **FP tuning** — path-based exclusion in `yara_scan.py` for 12 library prefixes (`androidx/`, `org/apache/`, `com/google/android/gms/`, `com/tom_roush/`, etc.). Tightened `Android_Crypto_StaticIV` rule.
4. **Pipeline results**:
   | APK | Before | After | Real | FPs |
   |-----|--------|-------|------|-----|
   | UnCrackable L1 | 1 | 1 | 1 | 0 |
   | UnCrackable L2 | 3 | 1 | 1 | 0 |
   | PIVAA | 7 | 6 | 6 | 0 |
   | InsecureBankv2 | 6 | 3 | 3 | 0 |
   | PennyWise (combo) | — | 4 | — | — |
5. **Jadx cache** — skip re-decompilation if `>10 .java` files exist.
6. **`.gitignore`**, **`requirements.txt`**, **`setup.md`** — replication files.

## Next
- Review PennyWise 4 findings (3 obfuscated-code + 1 AndroidX JNI)
- Run Notely (good APK) to validate 0 findings
- Fill `bank_whitelist.json` icon_phash via `register_icon.py`
