# Done — APK Sentinel Pipeline

**As of:** 2026-07-19 11:57:00

## Infrastructure
- `.gitignore` — env/, .env, artifacts, tools/, *.apk
- `requirements.txt` — all Python deps for replication
- `setup.md` — full setup guide (JDK, jadx, Ghidra, known quirks)

## L1 Combo + Ghidra (this session)
- Replaced naive string-scanner with YARA rule engine (apk_vulnerabilities.yar — 8 rules)
- Fixed `_scan_batch` instance truncation (`instances[:1]` lost cross-file matches)
- Fixed `scan_text` same bug
- Fixed `load_l0_evidence()` SHA256 from bytes, not filename stem
- Fixed `l1.py` track routing: `track1_jadx_then_track2_ghidra` now routes to combo_analyze
- Fixed track extraction: nested under `l0.routing`, not top-level
- Ghidra engine rewritten: extracts native .so from APK, runs analyzeHeadless per .so, YARA-scans exported strings
- JDK 21 installed via scoop for Ghidra 12.1.2

## L1 Validation (6 APKs)
| APK | Type | Findings | Real Vulns |
|-----|------|----------|------------|
| PIVAA | vuln | 7 | 4 (WebView JS, SSL TrustAll, Static IV, Dynamic Code Loading) |
| InsecureBankv2 | vuln | 6 | 3 (WebView JS, Static IV, Hardcoded pw) |
| UnCrackable L1 | vuln | 1 | 1 (hardcoded secret) |
| UnCrackable L2 | vuln | 2 | 0 (native obfuscation) |
| PennyWise | good | 9 | 0 (all FP from malware rules) |
| Notely | good | 2 | 0 (FP from malware rules) |
