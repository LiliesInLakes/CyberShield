# To Be Done — APK Sentinel Pipeline

**As of:** 2026-07-19 02:30:57

## Immediate (L1 finish)
- [ ] Re-run L1 jadx on InsecureBankv2 after library-filter fix; confirm summary sane
- [ ] Execute L1 ghidra engine on a sample; verify analyzeHeadless works (strings export)
- [ ] Execute L1 combo engine; verify merge + dedup
- [ ] Get a REAL banker sample (e.g. takopii 4 specimens, or re-acquire MalwareDatabase zips temp-only) to validate detection actually fires on malware behavior, not just framework noise
- [ ] Tune `SUSPICIOUS_SIGS` (schema.py) based on real-sample hits; add confidence scoring
- [ ] Fill `bank_whitelist.json` icon_phash via register_icon.py so L0 impersonation is live
- [ ] Build `L0/phash_compare.py` — score dataset/ icons vs whitelist (deferred earlier)

## L2 (dynamic/runtime) — not started
- [ ] Design L2: consumes L1 analysis.json; runtime/emulator behavioral analysis
- [ ] Define L2 input contract from L1Report schema

## Policy / hygiene
- [ ] Confirm L1 never persists live APK; works on artifacts only (or APK supplied at call time, discarded after)
- [ ] Add L1 findings → L0 evidence cross-link
- [ ] Document tool-setup steps (JDK17 + jadx CLI class quirk) in a setup.md

## Questions to resolve
- [ ] Where do real banker APKs come from for L1 validation without violating no-live-malware-on-disk policy? (temp-unpack + analyze + discard, or use safe test specimens like takopii)
- [ ] L2 emulator choice (AVD / rooted device / MobSF-style)
