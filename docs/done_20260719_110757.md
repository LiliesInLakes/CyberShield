# Done — APK Sentinel Pipeline

**As of:** 2026-07-19 11:07:57

## L0 (completed earlier sessions)
- `L0/ingest.py` — hashes, manifest, icon pHash, impersonation, routing, VT/MB lookups
- `L0/bank_whitelist.json` — 4 trusted banks (icon_phash null, needs fill)
- `L0/threat_cache.json` — 85 offline reputation hashes
- `L0/seed_cache.py`, `L0/harvest_dataset.py`, `L0/register_icon.py`

## L1 YARA Engine (this session)
- Replaced `SUSPICIOUS_SIGS` string-matching with YARA rule engine
- Created `L1/yara_templates/` with 9 rule files + `index.yar`
- 8 new vulnerability rules in `apk_vulnerabilities.yar`: WebView JS, SSL TrustAll, Weak Crypto, Static IV, WorldReadable storage, Dynamic Code Loading, Hardcoded Secrets
- `scan_sources()` optimised: batch concatenation (500 files/batch) + optional threading — 24k files in 11s vs timeout
- `_filter_scope()` — rules with `scope = "apk"` excluded from source compilation
- `_strip_byte_checks()` — removes filesize/uint32/hex checks for source scanning
- `_find_file_by_offset()` — binary search offset-to-file resolution in batches
- Fixed `load_l0_evidence()` to compute SHA256 from APK bytes, not filename stem
- Fixed `_scan_batch` instance truncation (`instances[:1]` lost cross-file matches)

## L1 Tested on 6 APKs
- **PIVAA** (vuln): 7 findings — 4 real (WebView JS, SSL TrustAll, Static IV, Dynamic Code Loading), 3 FP
- **InsecureBankv2** (vuln): 6 findings — 3 real (WebView JS, Static IV, Hardcoded pw), 3 FP (Google Play Services)
- **UnCrackable L1** (vuln): 1 finding — 1 real (hardcoded root-bypass secret)
- **UnCrackable L2** (vuln): 2 findings — 0 real (obfuscation in native `.so`, java layer clean)
- **PennyWise** (good): 6 findings — 0 real (all FP from malware rules on SDK/lib code)
- **Notely** (good): 2 findings — 0 real (FP from malware rules)
