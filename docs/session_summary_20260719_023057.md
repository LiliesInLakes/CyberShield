# Session Summary — 2026-07-19 02:30:57

## Goal
Progress the APK Sentinel pipeline from L0 (triage, done) into L1 (static deep-analysis: jadx + ghidra).

## What happened

### L0 → L1 design
- Agreed L0 decides routing track (`track1_jadx` / `track2_ghidra` / combo); L1 only consumes that decision via `evidence.json`. L1 must NOT re-derive.
- L1 accepts an APK path at call time (live malware not stored). Tested on `testing_apps/InsecureBankv2.apk` (safe vulnerable app).
- Architecture: `l1.py` dispatcher + 3 engine modules (jadx / ghidra / combo) + shared `schema.py` (uniform L1Finding for L2 to ingest).

### Tooling setup
- System Java is v8 — too old for jadx 1.5.6 (needs 11+). Downloaded portable **JDK17** to `D:\BOI\tools\jdk17\`.
- Downloaded **jadx 1.5.6** (headless jar) and **ghidra 12.1.2** (user extracted to `D:\BOI\tools\ghidra\ghidra_12.1.2_PUBLIC\`).
- Key quirk: on Windows, `java -jar jadx-...jar` launches the GUI. Must invoke explicit class `jadx.cli.JadxCLI` via `-cp`. Fixed in code.

### Code built
- `L1/schema.py`: L1Finding/L1Report dataclasses, Severity/Category enums, `SUSPICIOUS_SIGS` table (SMS intercept, overlay, accessibility abuse, C2, exfil, native payload, etc.), `classify_string()`, `load_l0_evidence()`.
- `L1/engines/jadx_analyze.py`: headless decompile + source scan.
- `L1/engines/ghidra_analyze.py`: analyzeHeadless string export + scan.
- `L1/engines/combo_analyze.py`: union of both.
- `L1/l1.py`: dispatcher reading L0 evidence, routing by track, writing `L1/artifacts/<sha256>/analysis.json`.

### Bug fixed
- jadx scanner flagged app code `com/android/insecurebankv2/...` as library noise (substring `android/` match). Rewrote `_is_library` to check only the leading path segment. Now app findings surface correctly.

### Verification
- jadx decompiles InsecureBankv2 → 2992 Java files, headless, no GUI. L1 dispatcher runs, writes analysis.json (246 raw findings before filter; post-fix app-code findings = 0 because InsecureBankv2 is a vulnerable app, not a banker — expected).

## Decisions
- L1 takes APK path per-call; no live malware storage.
- Separate engine files per tool; dispatcher picks based on L0 track.
- User handles ghidra extraction; confirmed nested path.

## Next
- Re-verify jadx findings post-filter.
- Run ghidra + combo engines.
- Obtain a real banker sample to validate detection fires on actual malware behavior.
- Move to L2 (dynamic/runtime).

## Artifacts
- `D:\BOI\docs\status_20260719_023057.md`
- `D:\BOI\docs\done_20260719_023057.md`
- `D:\BOI\docs\toBedone_20260719_023057.md`
- `D:\BOI\docs\session_summary_20260719_023057.md`
