# Session Summary — 2026-07-19 11:07:57

## Goal
Replace naive `SUSPICIOUS_SIGS` L1 scanner with YARA rule engine, test on vulnerable + good APKs, tune rules.

## Achievements
1. **YARA engine** (`L1/engines/yara_scan.py`): APK/source/text modes, batch concatenation (500 files/batch), offset binary-search file resolution, thread-pool support. 24k Java files in 11s vs >300s timeout.
2. **Vulnerability rules** (`apk_vulnerabilities.yar`): 8 rules targeting Android anti-patterns — WebView JS, SSL TrustAll, weak crypto, static IV, world-readable storage, dynamic code loading, hardcoded secrets.
3. **Testing**: All 6 APKs scanned. PIVAA caught 4/4 real vulns. InsecureBankv2 caught 3 real vulns. UnCrackable L1 caught 1 hardcoded secret.
4. **Bug fixes**: `load_l0_evidence()` SHA256 fix, `_scan_batch` instance truncation fix, `_strip_byte_checks()` fixed for source rules.

## Next
1. Run Ghidra engine on PIVAA (native `.so` analysis)
2. Run combo engine (jadx + ghidra merge)
3. Test on PennyWise (has native libs)
4. Move to L2 (dynamic)
